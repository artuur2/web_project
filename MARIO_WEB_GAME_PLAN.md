# План разработки веб-игры в стиле Mario

## 1) Определить цели и MVP
- Выбрать формат: 2D платформер в браузере.
- Зафиксировать минимальную версию (MVP):
  - 1 уровень;
  - персонаж с движением влево/вправо и прыжком;
  - платформы, препятствия, монеты;
  - враги с базовой логикой;
  - условие победы/поражения;
  - счёт и экран конца уровня.
- Описать ограничения по срокам, качеству и устройствам (desktop/mobile).

## 2) Выбрать стек и архитектуру
- Рендер: Canvas 2D (проще старт), при необходимости позже перейти на Phaser.
- Технологии: HTML/CSS/TypeScript (или JS), сборка через Vite.
- Архитектура модулей:
  - `core` (игровой цикл, время, сцены);
  - `entities` (игрок, враги, предметы);
  - `physics` (гравитация, коллизии);
  - `level` (тайлы, загрузка карты);
  - `ui` (HUD, меню, экраны).

## 3) Подготовить базовый проект
- Инициализировать проект и настроить линтер/форматтер.
- Добавить структуру папок (`src/core`, `src/entities`, `src/levels`, `src/assets`, `src/ui`).
- Подключить базовую страницу с Canvas и resize-поведение.
- Настроить ассеты (спрайты, тайлсеты, звуки) и их предзагрузку.

## 4) Реализовать игровой цикл
- Ввести фиксированный `update` + `render` (или semi-fixed timestep).
- Добавить менеджер сцен: `Boot`, `Menu`, `Level1`, `GameOver`, `Win`.
- Обработать паузу, перезапуск, переходы между сценами.

## 5) Реализовать игрока
- Компоненты игрока:
  - позиция/скорость;
  - ускорение, гравитация, трение;
  - состояния (`idle`, `run`, `jump`, `fall`, `hurt`).
- Управление: клавиатура (`A/D` или стрелки, `Space`/`W` для прыжка).
- Ограничить прыжок (coyote time/буфер ввода — опционально для лучшего UX).

## 6) Реализовать физику и коллизии
- Типы коллизий: игрок↔платформы, игрок↔враги, игрок↔монеты, игрок↔ловушки.
- Определение пересечений по AABB.
- Разделение коллизий по осям (X/Y), чтобы избежать “залипания”.
- Логика “прыжок на врага сверху” vs урон при боковом касании.

## 7) Сделать уровень
- Формат уровня (JSON или tilemap):
  - геометрия платформ;
  - позиции врагов;
  - коллектаблы;
  - точка старта/финиша.
- Камера, следящая за игроком (с ограничением по границам карты).
- Параллакс-фон и базовые визуальные слои.

## 8) Добавить врагов и интерактив
- 1–2 типа врагов:
  - патруль по платформе;
  - разворот на краю/стене.
- Монеты и бонусы (очки/доп. жизнь).
- Чекпоинты (опционально для MVP+).

## 9) HUD и UX
- Показать очки, жизни, таймер.
- Экран паузы, рестарт уровня, экран победы/поражения.
- Добавить простые анимации UI и звуковые эффекты.

## 10) Звук и “ощущение игры” (game feel)
- SFX: прыжок, сбор монет, урон, победа.
- Музыка для уровня и меню.
- Полировка: screen shake, hit-stop, частицы при сборе/ударе.

## 11) Тестирование
- Ручной чеклист:
  - стабильный FPS;
  - нет провалов сквозь платформы;
  - корректные переходы сцен;
  - управление отзывчивое.
- Авто-проверки:
  - unit-тесты логики коллизий;
  - smoke-тест запуска и рендера базовой сцены.
- Проверка в Chrome/Firefox/Safari, на разных разрешениях.

## 12) Деплой и аналитика
- Сборка production-версии.
- Деплой (Vercel/Netlify/GitHub Pages).
- Подключить базовую аналитику: старт уровня, смерть, завершение.

## 13) План итераций после MVP
- Новые уровни и типы врагов.
- Система достижений.
- Мобильное управление (touch controls).
- Таблица рекордов (backend + auth при необходимости).

---

## Рекомендованный порядок на 2 недели (пример)
- **Дни 1–2:** проект, цикл, сцены, Canvas, ассеты.
- **Дни 3–4:** игрок + физика + коллизии.
- **Дни 5–6:** уровень 1 + камера + враги.
- **День 7:** HUD + состояния победы/поражения.
- **Дни 8–9:** звук + полировка.
- **Дни 10–11:** тесты и багфиксы.
- **Дни 12–14:** деплой + финальная шлифовка.
